package datastructures.arrays.project;

public class Two_1 {
    /**
     * HighArray class
     *      > add method called getMax() that returns the value of the highest key in the array, or -1 if the array is empty
     *      > add some code in main() to exercise this method
     *      > assume all the keys are positive numbers
     */

}
